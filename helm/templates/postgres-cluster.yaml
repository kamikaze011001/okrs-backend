apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ include "okrs-backend.fullname" . }}-pg
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:15.4
  storage:
    size: 2Gi
  bootstrap:
    initdb:
      database: "okrs_db"
      owner: "okrs_user"