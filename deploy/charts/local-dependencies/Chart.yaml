apiVersion: v2
name: local-dependencies
description: Local-only dependencies for the OKRs backend
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: valkey
    version: "0.10.0"
    repository: "https://valkey.io/valkey-helm/"
    condition: valkey.enabled
