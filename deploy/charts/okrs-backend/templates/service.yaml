apiVersion: v1
kind: Service
metadata:
  name: {{ include "okrs-backend.fullname" . }}
  labels:
    {{- include "okrs-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "okrs-backend.selectorLabels" . | nindent 4 }}